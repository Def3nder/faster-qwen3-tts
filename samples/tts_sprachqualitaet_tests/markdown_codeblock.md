# Markdown-Codeblock

Vor dem Codeblock steht ein normaler Satz.

```python
def begruessung(name):
    return f"Hallo, {name}!"

print(begruessung("Welt"))
```

Nach dem Codeblock folgt wieder ein normaler Satz.
